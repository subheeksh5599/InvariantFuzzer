[package]
name = "attacker-oracle"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "attacker_oracle"

[dependencies]
pinocchio = { version = "0.10", features = ["cpi"] }
